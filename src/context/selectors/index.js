import * as satelliteSelect from './components/satellitesSelect';
import * as satellites from './data/satellites';

export default {
    data: {
        satellites
    },
    components: {
        satelliteSelect
    }
}